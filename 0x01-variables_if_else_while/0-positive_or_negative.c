#include<stdio>
#include<time.h>
#include<stdio.h>
/**
 * main - Start point
 * Return = Always (0)
 */
int main(void)
{
	int i;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
}
if (i > 0)
{
	printf("%d is positive\n", i);
}
else if  (i == 0)
{
	printf("%d is zero\n" i);
}
while (i < 0)
{
	printf("%d is negative\n" i);
}
{
	/* your code goes there */
	return (0);
}
